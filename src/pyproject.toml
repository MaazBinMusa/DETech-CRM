[project]
name = "detech-crm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2.0",
    "python-dotenv>=1.2.3",
    "streamlit>=1.62.0",
    "supabase>=2.31.0",
]

[project.scripts]
detech-crm = "detech_crm:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
